{% extends 'base.html.twig' %}

{% block title %}CourseCategory{% endblock %}

{% block body %}
    <h1>CourseCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ course_category.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ course_category.createdAt ? course_category.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ course_category.updatedAt ? course_category.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_course_category_index') }}">back to list</a>

    <a href="{{ path('app_course_category_edit', {'id': course_category.id}) }}">edit</a>

    {{ include('Frontend/course_category/_delete_form.html.twig') }}
{% endblock %}
